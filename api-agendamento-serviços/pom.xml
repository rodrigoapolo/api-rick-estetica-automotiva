<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.7</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.automotiva.estetica.rick</groupId>
	<artifactId>api-agendamento-servicos</artifactId>
	<version>1.0.1-SNAPSHOT</version>
	<name>api-agendamento-servicos</name>
	<description>Esta API tem como objetivo gerenciar o agendamento de serviços da RICK Estética Automotiva.</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<argLine></argLine>
		<surefireArgLine></surefireArgLine>
		<failsafeArgLine></failsafeArgLine>
		<pmd.skip>false</pmd.skip>
		<!-- Versões de dependências críticas (melhor controle e rastreabilidade) -->
		<mapstruct.version>1.6.3</mapstruct.version>
		<springdoc-openapi.version>2.8.9</springdoc-openapi.version>
		<jjwt.version>0.12.7</jjwt.version>
		<google-api-client.version>2.8.1</google-api-client.version>
		<google-oauth-client.version>1.37.0</google-oauth-client.version>
		<google-http-client.version>1.45.2</google-http-client.version>
		<google-api-services-calendar.version>v3-rev20230825-2.0.0</google-api-services-calendar.version>
		<maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
		<spotless-maven-plugin.version>2.44.0</spotless-maven-plugin.version>
		<maven-pmd-plugin.version>3.26.0</maven-pmd-plugin.version>
		<jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>
		<lombok.version>1.18.38</lombok.version>
		<h2.version>2.2.224</h2.version>
		<mysql-connector.version>8.3.0</mysql-connector.version>
		<flyway.version>10.18.1</flyway.version>
	</properties>
	<repositories>
		<repository>
			<id>local-libs</id>
			<url>file://${project.basedir}/libs</url>
		</repository>
	</repositories>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>com.automotiva.estetica.rick</groupId>
			<artifactId>librabbitmq</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>
		<!-- H2: mantido apenas para testes (profiles test / integration-test) -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<version>${h2.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- MySQL Connector/J para profiles dev, homolog e prod -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<version>${mysql-connector.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.tngtech.archunit</groupId>
			<artifactId>archunit-junit5</artifactId>
			<version>1.4.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-amqp</artifactId>
		</dependency>

		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
			<version>1.6.3</version>
		</dependency>

		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc-openapi.version}</version>
		</dependency>


        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
		    <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>


		<!-- Flyway: versionamento e migração de schema -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
			<version>${flyway.version}</version>
		</dependency>
		<!-- flyway-mysql obrigatório para MySQL 8+ (driver dedicado) -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-mysql</artifactId>
			<version>${flyway.version}</version>
		</dependency>

		<!-- Spring Data Redis: Cache distribuído com Redis -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>@{surefireArgLine} ${argLine}</argLine>
					<forkCount>1</forkCount>
					<reuseForks>true</reuseForks>
					<!-- Exclui arquivos *IT.java do surefire (unitários apenas) -->
					<excludes>
						<exclude>**/*IT.java</exclude>
					</excludes>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<configuration>
					<argLine>@{failsafeArgLine} ${argLine}</argLine>
					<forkCount>1</forkCount>
					<reuseForks>true</reuseForks>
					<!-- Inclui todos os testes de integração (*IT.java) -->
					<includes>
						<include>**/*IT.java</include>
					</includes>
					<!-- Garante que falha no IT falha o build -->
					<failIfNoTests>false</failIfNoTests>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco-maven-plugin.version}</version>
				<executions>
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<propertyName>surefireArgLine</propertyName>
							<destFile>${project.build.directory}/jacoco-ut.exec</destFile>
							<append>false</append>
							<includes>
								<include>com.automotiva.estetica.rick.*</include>
							</includes>
							<excludes>
								<exclude>*HibernateProxy*</exclude>
								<exclude>*HibernateInstantiator*</exclude>
								<exclude>*MockitoMock*</exclude>
								<exclude>*$auxiliary$*</exclude>
								<exclude>**/*MapperImpl*</exclude>
								<exclude>**/*$HibernateProxy*</exclude>
								<exclude>**/*$HibernateInstantiator*</exclude>
							</excludes>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-prepare-agent-integration</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>prepare-agent-integration</goal>
						</goals>
						<configuration>
							<propertyName>failsafeArgLine</propertyName>
							<destFile>${project.build.directory}/jacoco-it.exec</destFile>
							<append>false</append>
							<includes>
								<include>com.automotiva.estetica.rick.*</include>
							</includes>
							<excludes>
								<exclude>*MockitoMock*</exclude>
								<exclude>*$auxiliary$*</exclude>
								<exclude>**/*MapperImpl*</exclude>
								<exclude>**/*$HibernateProxy*</exclude>
								<exclude>**/*$HibernateInstantiator*</exclude>
							</excludes>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-merge</id>
						<phase>post-integration-test</phase>
						<goals>
							<goal>merge</goal>
						</goals>
						<configuration>
							<destFile>${project.build.directory}/jacoco.exec</destFile>
							<fileSets>
								<fileSet>
									<directory>${project.build.directory}</directory>
									<includes>
										<include>jacoco-*.exec</include>
									</includes>
								</fileSet>
							</fileSets>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco.exec</dataFile>
						</configuration>
					</execution>
					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco.exec</dataFile>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.75</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.65</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-compiler-plugin</artifactId>
			<version>${maven-compiler-plugin.version}</version>
			<configuration>
				<source>21</source>
				<target>21</target>
				<annotationProcessorPaths>
					<path>
						<groupId>org.projectlombok</groupId>
						<artifactId>lombok</artifactId>
						<version>${lombok.version}</version>
					</path>
					<path>
						<groupId>org.mapstruct</groupId>
						<artifactId>mapstruct-processor</artifactId>
						<version>${mapstruct.version}</version>
					</path>
				</annotationProcessorPaths>
			</configuration>
		</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<mainClass>com.automotiva.estetica.rick.ApiAgendamentoApplication</mainClass>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-pmd-plugin</artifactId>
			<version>${maven-pmd-plugin.version}</version>
			<configuration>
				<skip>${pmd.skip}</skip>
				<minimumTokens>120</minimumTokens>

				<!-- Falha o build se encontrar violações -->
				<failOnViolation>true</failOnViolation>

				<!-- Mostra as violações no console -->
				<printFailingErrors>true</printFailingErrors>

				<!-- Evita gerar links HTML desnecessários -->
				<linkXRef>false</linkXRef>

				<!-- Opcional: define prioridade mínima para alertas (1=crítico, 5=leve) -->
				<minimumPriority>4</minimumPriority>
			</configuration>

			<executions>
				<execution>
					<phase>verify</phase>
					<goals>
						<goal>check</goal>
						<goal>cpd-check</goal> <!-- também bloqueia se encontrar código duplicado -->
					</goals>
				</execution>
			</executions>
		</plugin>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>${spotless-maven-plugin.version}</version>

				<configuration>
					<java>
						<eclipse>
							<file>${project.basedir}/lint-config/eclipse-java-formatter.xml</file>
						</eclipse>

						<removeUnusedImports/>
						<trimTrailingWhitespace/>
						<endWithNewline/>
					</java>
				</configuration>

				<executions>
					<execution>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>

	</build>

	<profiles>
		<profile>
			<id>skip-pmd-on-jdk22plus</id>
			<activation>
				<jdk>[22,)</jdk>
			</activation>
			<properties>
				<pmd.skip>true</pmd.skip>
			</properties>
		</profile>
	</profiles>

</project>
