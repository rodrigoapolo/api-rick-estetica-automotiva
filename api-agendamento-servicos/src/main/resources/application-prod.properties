# =====================================================
# Profile: prod — MySQL produção
# TODOS OS SECRETS OBRIGATORIAMENTE VIA ENV VAR
# NUNCA HARDCODE de senhas em produção!
# 
# Exemplo de ENV VARs a definir:
#   DB_URL=jdbc:mysql://db-host:3306/rick_prod?useUnicode=true&characterEncoding=UTF-8&serverTimezone=America/Sao_Paulo&useSSL=true
#   DB_USERNAME=rick_prod_user
#   DB_PASSWORD=<senha-forte-aqui>
#   JWT_SECRET=<base64-secret-aqui>
#   MAIL_USERNAME=sender@example.com
#   MAIL_PASSWORD=<senha-app-email>
#   RABBITMQ_USERNAME=prod_user
#   RABBITMQ_PASSWORD=<senha-rabbitmq>
#   ALLOWED_CORS_ORIGINS=https://app.example.com,https://admin.example.com
# =====================================================

# ...existing code...

# MySQL produção — variáveis de ambiente obrigatórias (sem defaults)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Pool HikariCP — configuração robusta para produção
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=60000

# JPA — NUNCA usar create-drop ou update em produção!
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# -----------------------------------------------------------
# Flyway — prod
# baseline-on-migrate=false em prod: o banco DEVE ser limpo ou
# já ter sido migrado antes. Sem seed de dados fictícios.
# -----------------------------------------------------------
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=false
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false
spring.flyway.clean-disabled=true

# Porta
server.port=8080

# Logs apenas erros
logging.level.com.automotiva.estetica.rick=WARN
logging.level.org.springframework.web=ERROR

# H2 Console — desabilitado em produção (usa MySQL)
spring.h2.console.enabled=false

# Proxy Reverso — confiar em headers X-Forwarded-* do proxy
server.tomcat.remoteip.remote-ip-header=X-Forwarded-For
server.tomcat.remoteip.protocol-header=X-Forwarded-Proto
server.tomcat.remoteip.port-header=X-Forwarded-Port
server.forward-headers-strategy=native

# Configurações de segurança
server.error.include-stacktrace=never
server.error.include-message=never
server.error.include-binding-errors=never

# CORS em prod — obrigatoriamente via variável de ambiente com domínios explícitos
# Exemplo: ALLOWED_CORS_ORIGINS=https://app.example.com,https://admin.example.com
security.cors.allowed-origins=${ALLOWED_CORS_ORIGINS}
security.cors.allow-credentials=true

# data.sql — nunca executar em produção
spring.sql.init.mode=never

